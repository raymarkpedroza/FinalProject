using Pastebook.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Pastebook.Managers
{
    public class NotificationManager
    {
        //PastebookServiceClient pastebookServiceClient = new PastebookServiceClient();
        //public int AddNotification(NotificationModel notification)
        //{
        //    AddNotificationRequest request = new AddNotificationRequest();
        //    request.Notification = Mapper.MapMVCNotificationModelToWCFNotificationEntity(notification);

        //    AddNotificationResponse response = new AddNotificationResponse();
        //    response = pastebookServiceClient.AddNotification(request);

        //    return response.Result;
        //}
    }
}